/** Clé API pour terminaux NFC / reconnaissance faciale (matériel de pointage). */
export declare function getDeviceApiKey(): string;
/** Échoue au démarrage si la clé matériel est absente ou faible en production. */
export declare function ensureDeviceApiKeyConfiguration(): void;
//# sourceMappingURL=device-api-key.util.d.ts.map